# Comma-separated browser origins allowed to call the API. In production the
# unified Cloud Run service serves both the SPA and API from one origin.
PUBLIC_WEB_ORIGINS=http://localhost:3000

# Optional local GGUF model. Without it the product uses the clearly-labelled
# synthetic educational candidate generator.
# MODEL_PATH=/absolute/path/to/model.gguf
